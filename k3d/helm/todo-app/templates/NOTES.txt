Todo app is installed.

  Get the app URL by running:
    kubectl port-forward svc/{{ include "todo-app.fullname" . }} 3000:{{ .Values.service.port }}
  Then open http://localhost:3000

  Backup CronJob: {{ if .Values.backupCron.enabled }}enabled (schedule: {{ .Values.backupCron.schedule }}){{ else }}disabled{{ end }}

  View CronJob runs:
    kubectl get jobs -l app.kubernetes.io/name={{ .Chart.Name }}
    kubectl logs job/<job-name>
